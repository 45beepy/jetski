# Blog posts

Add markdown files here for your blog.

Each file should be named with a slug, for example:

- `my-first-post.md`
- `how-i-built-this-portfolio.md`

The loader will use `posts.json` as a manifest to determine which files to display and in what order.
